import React, { useState } from 'react';
import {
    View,
    Text,
    TextInput,
    StyleSheet,
    TouchableOpacity,
    Image,
    SafeAreaView,
} from 'react-native';

const SearchScreen = () => {
  

    return (
        <SafeAreaView style={{}}>
            
        </SafeAreaView>
    );
};

export default SearchScreen;
