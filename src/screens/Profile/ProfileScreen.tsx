import React, { useState } from 'react';
import {
    View,
    Text,
    TextInput,
    StyleSheet,
    TouchableOpacity,
    Image,
    SafeAreaView,
} from 'react-native';

const ProfileScreen = () => {
  

    return (
        <SafeAreaView style={{}}>
            
        </SafeAreaView>
    );
};

export default ProfileScreen;
